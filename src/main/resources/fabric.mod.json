{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${Id}",
	"description": "Extended resourcepack customizability.",
	"authors": [ "ianaduarte" ],
	"contact": {
		"homepage": "https://modrinth.com/${id}",
		"sources": "https://github.com/ianaduarte/${id}"
	},
	"license": "ARR",
	"icon": "assets/icon.png",
	"environment": "*",
	"entrypoints": {
		"client": ["${group}.${id}.client.${Id}Client"]
	},
	"mixins": ["${id}.mixins.json"],
	"depends": {
		"minecraft": "~1.21.1",
		"java": ">=21"
	}
}
